# `intellipy.dump`

The recording tool behind the `intellipy-dump` command. Its pieces — the wave
selection and the per-kind writers — are importable if you want a variation on it
rather than a copy of it.

```{eval-rst}
.. automodule:: intellipy.dump
   :members:
   :undoc-members:
   :show-inheritance:
```
